/* eslint-disable @typescript-eslint/no-explicit-any */
import { FC } from "react";

type Props = {
  trip: any;
};

export const Footer: FC<Props> = ({ trip }) => {
  return (
    <div>
      <h2></h2>
    </div>
  );
};
